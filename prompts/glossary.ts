export const P_SYS_GLOSSARY = `<术语与边界定义>
# 【术语与边界定义】

1. **logs vs tavern_commands**
- logs：只写叙事与对白，禁止系统词汇与数值结算。
- tavern_commands：只写状态更新，必须与 logs 一一对应。

2. **拾取 vs 发现**
- 发现/看见/掉落：只写叙事，不生成物品指令。
- 拾取/装入/分配/交给背负者：必须生成对应指令。

3. **背包 / 公共战利品 / 战利品仓库**
- 背包：个人随身携带物品。
- 公共战利品：探索中已拾取但未分配的临时战利品。
- 战利品：已归档到眷族/仓库的战利品。

4. **时间与地点**
- 时间格式："第X日 HH:MM"；日期为 "YYYY-MM-DD"。
- 位置变化必须同步 \`gameState.当前地点\` 与 \`gameState.世界坐标\`。

5. **NPC后台跟踪**
- 当 NPC 离场执行任务/约定未来行动时，必须记录到 \`gameState.世界.NPC后台跟踪\`。
- 当 NPC 返回或行动结束，应清理对应条目。

6. **手机联动**
- 若启用手机独立API：当剧情涉及手机聊天/论坛/通知时使用 \`phone_sync_plan\`，手机内容不要写入 \`tavern_commands\`。
- 若未启用：禁止输出 \`phone_sync_plan\`，改用 \`tavern_commands\` 更新手机内容。
- \`phone_sync_plan\` 为**规划意图**（对象/触发/节奏/语气），允许基于 phone context 制定，不要求 logs 中出现具体消息内容。
- logs 禁止复述手机正文，仅可描述“发送/收到/查看/等待”等动作或提示。

7. **情报笔记 (gameState.笔记)**
- 默认由玩家手动维护。
- 若玩家在 [用户指令] 中明确要求记录线索，允许使用 push 更新笔记。
</术语与边界定义>`;



